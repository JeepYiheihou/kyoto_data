/* Data state struct with all the data structure related state info. */
pub mod data_info;

/* Entry of the whole collection of supported data structures.
 * This initialized at the start of server. */
pub mod db;